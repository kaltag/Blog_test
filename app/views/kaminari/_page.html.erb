<% if page.current? %>
  <span class="bg-green-500 text-white px-4 py-3 rounded"><%= page %></span>
<% else %>
  <%= link_to page, url, remote:, rel: page.rel, class: 'px-4 text-gray-800 font-light' %>
<% end %>
